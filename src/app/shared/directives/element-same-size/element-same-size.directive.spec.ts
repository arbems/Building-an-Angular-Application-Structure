import { ElementSameSizeDirective } from './element-same-size.directive';

describe('ElementSameSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementSameSizeDirective();
    expect(directive).toBeTruthy();
  });
});
